# spring.application.name=visitor
# Server
server.port=8080
server.servlet.context-path=/api
app.base-url=http://localhost:8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/visitor_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=visitor_user
spring.datasource.password=visitor123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration - REMOVE the dialect line
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# # JPA Configuration
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.properties.hibernate.format_sql=true

# Email Configuration (for OTP)
spring.mail.host=smtp.rediffmailpro.com
spring.mail.port=587
spring.mail.username=developer@mayaa.money
spring.mail.password=S@&37abl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT Configuration
jwt.secret=mySuperSecretKeyForJWTTokenGeneration12345678901234567890
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# File Upload Configuration
file.upload-dir=./visitors_images
file.max-size=5242880

# OTP Configuration
otp.expiration-minutes=5
otp.length=6

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Logging
logging.level.com.paulaccesso=DEBUG
logging.level.org.springframework.security=DEBUG